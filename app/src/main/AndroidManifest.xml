<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.hitej.android.metalarchives"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MetalArchivesActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">.
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MASearchActivity"
                  android:label="Search Activity"
                  android:parentActivityName=".MetalArchivesActivity" />

        <activity android:name=".BandInfoActivity"
                  android:label="Band Info Activity"
                  android:parentActivityName=".MASearchActivity" />
        <activity android:name=".MASearchResultsActivity"
            android:label="Search Results Activity"
            android:parentActivityName=".MASearchActivity" />
        <activity android:name=".DiscInfoActivity"
                  android:label="Disc Info Activiy"
                  android:parentActivityName=".BandInfoActivity"/>
    </application>

</manifest>
